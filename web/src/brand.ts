/**
 * O NOME DO PRODUTO MORA AQUI, E SÓ AQUI.
 *
 * "Circula" vem de circulação — o nome que a própria biblioteca dá ao
 * movimento de empréstimo e devolução. É verbo, não objeto: o que este
 * sistema faz é manter o acervo andando.
 *
 * Trocar leva um minuto: mude as linhas abaixo e nada mais no código precisa
 * ser tocado. Nenhum outro arquivo escreve o nome literalmente.
 */
export const brand = {
  /** Nome curto, usado no cabeçalho e no título da aba. */
  name: 'Circula',
  /** O que o produto é, em uma linha. */
  tagline: 'Biblioteca em movimento',
  /** Nome de quem opera, no vocabulário da casa. */
  surface: 'atendimento',
} as const;
