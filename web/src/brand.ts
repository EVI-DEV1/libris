/**
 * O NOME DO PRODUTO MORA AQUI, E SÓ AQUI.
 *
 * Foi criado como nome provisório e deve poder ser trocado em um minuto:
 * mude as três linhas abaixo e nada mais no código precisa ser tocado.
 * Nenhum outro arquivo escreve "Lombada" literalmente.
 */
export const brand = {
  /** Nome curto, usado no cabeçalho e no título da aba. */
  name: 'Lombada',
  /** O que o produto é, em uma linha. Aparece na tela de entrada. */
  tagline: 'operação de balcão',
  /** Nome de quem opera, no vocabulário da casa. */
  surface: 'balcão',
} as const;
